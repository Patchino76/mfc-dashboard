/** @type {import('next').NextConfig} */
const nextConfig = {
  // Next.js now handles CSS modules automatically
  // Just enable experimental features if needed
  experimental: {
    optimizeCss: true // This is optional
  }
};

module.exports = nextConfig;
